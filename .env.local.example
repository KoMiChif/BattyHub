NEXT_PUBLIC_API_URL=http://localhost:4000

# Google OAuth — see GOOGLE_OAUTH_SETUP.md. Same Client ID is used by
# the backend (GOOGLE_CLIENT_ID). Leave blank to disable the Google button.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
